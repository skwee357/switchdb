import { Brand } from '../../../switch';
import boxJade from './box-jade';
import boxNavy from './box-navy';

export default {
    name: 'NovelKeys',
    switches: [boxJade, boxNavy],
} satisfies Brand;
